# CafeList
CafeList is a .NET console café menu that builds lists of drinks, foods and syrups with dietary tags, prints them in sections and even auto generates a QR code linking to a blank page. It’s a simple showcase for data modeling, filtering and console output.
